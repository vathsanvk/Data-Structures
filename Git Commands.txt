git clone //clones all branches from remote to local by creating a local repo
git checkout <dev/master>  // use this to do operation on corresponding branches
git pull -A // pulls all branches from remote and merges with corresponding branch in local
-> git status 
-> git add <filename>
-> git status
->git commit -m "<comments>"
->git log
->git push -u origin master
